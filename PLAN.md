# PLAN — 식품 표시·규격 라우팅 에이전트 (agentteam-m05)

> 사용자 관점 요구는 [PRD](PRD.md). 과제 원문은 PRD 최상단 LMS 링크.
> 슬라이스 하나 = 세션 하나. 끝나면 여기 체크하고 커밋한다.
> 구현 세션 시작 프롬프트: `PRD.md, PLAN.md 읽고 슬라이스 N 진행. 끝나면 PLAN.md 체크하고 커밋.`


## Context

아이펠 **에이전트 팀 꾸리기_Agt1** 5번 노드 「고객 응대 에이전트 만들기 [프로젝트]」.
과제 구조는 고정이다 — `입력 → 카테고리 판정 → 카테고리별 근거 조립 → 근거만으로 답변 → 검증`,
그리고 확신이 없으면 넘기기. 요구사항 전체는
`agentteam-m05-customer-support-agent/PRD.md`.

도메인은 **음료류 식품 법령 안내**. 유형 정의가 겹쳐 대표가 품목제조보고에서 실제로 헷갈리고,
함량 %·사용량 한도처럼 **모델이 자신 있게 지어내는 숫자**가 깔려 있어 환각 검증이
장식이 아니라 실사용 조건이 된다.

### 계획을 세우며 확인한 것 — 전제 하나가 뒤집혔다

**`m05-rag-chatbot-service` 가 같은 도메인의 직계 선행 구현이다.** 식품 표시·광고 법령
RAG 챗봇이고, 코퍼스에 아래가 **이미 수집·청킹돼 있다** (수집일 2026-08-28, 365청크 + 별표 6종):

| 코드 | 문서 | 청크 |
|---|---|---|
| FLA / FLD / FLR | 식품 등의 표시·광고에 관한 법률 · 시행령 · 시행규칙 | 64 / 20 / 80 |
| LBL | **식품등의 표시기준** (고시) | 126 + 별표 |
| UNF | 식품등의 부당한 표시 또는 광고의 내용 (고시) | 18 + 별표 |
| ALG · NUT · GMO · REV | 알레르기·나트륨·GMO·사전심의 고시 | 57 |

그래서 **원래 계획의 playwright 전제는 틀렸다.** m05 는 스크래핑을 하지 않는다 —
**국가법령정보센터 OPEN API**(`law.go.kr/DRF/lawService.do`)를 직접 호출해 JSON 을 받는다.
README 에 *"HTML 파싱 불필요. 스크래핑하면 1.3KB 껍데기만 온다"* 고 적혀 있다.
`LAW_OC` 환경변수가 없으면 `'test'` 로 도는 것도 확인했다 — **키 없이 시작할 수 있다.**

단, 내가 직접 확인한 대로 **식품공전과 식품첨가물공전은 이 API 로 못 받는다**
(법제처 본문 329자, 실질 내용이 전부 첨부파일). 이 둘만 식품안전나라에서 따로 받아야 한다.

→ **수집이 두 갈래로 갈린다.** 슬라이스 1(API)과 슬라이스 2(브라우저)로 나눈다.

### m05·m04·agentteam-m01 에서 가져올 것

언어가 다르다 — m05 는 TypeScript, 이번은 Python. **코드가 아니라 설계와 파싱 규칙을 옮긴다.**

- **법령 청킹 규칙** (`m05-rag-chatbot-service/app/scripts/fetch-corpus.mjs`) — 조/항/호 계층을
  사실 단위로 쪼개고, 조 번호 없는 고시는 본문에서 위치를 읽어 `path` 를 만든다.
  위치를 모르면 `본문` 으로 두고 **없는 호 번호를 만들어내지 않는다**
- **청크 스키마** — `{ id, source, sourceKind, lawName, path, text, url, effectiveDate, inForce }`
- **`eflaw` 로 현행 시행본만 받고 미시행 개정은 알림만** — PRD 의 "현행 시행본, 미시행 제외"와 그대로 맞물린다
- **평가셋 스키마** (`app/scripts/eval-set.mjs`) — `gold`(정답 청크 ID) + **`goldCheck`(정규식)**.
  코퍼스가 바뀌어 ID 가 밀리면 잡아낸다. 이 장치를 그대로 가져온다
- **이중 채점기** (`app/src/lib/judge.ts`) — 규칙 층(인용 유효성·거절 여부)은 결정적으로,
  LLM 층은 JSON 스키마 강제 + 온도 0 으로 근거성만
- **실험 축 하나만 바꾸는 가드** (`eval-run.mjs`, 두 축 이상이면 `exit 2`) —
  과제의 *"개선은 한 번에 하나씩"* 요구에 그대로 맞는다
- **LangGraph 관행** (`agentteam-m01-langgraph-workflow`) — State 는 `TypedDict`,
  노드는 `(state) -> 부분 딕셔너리`. **`compile()` 은 재정의된 함수를 다시 잡지 않는다**
  (같은 이름으로 다시 정의해도 `StateGraph(...)` 부터 새로 만들어야 반영된다 — 조용히 옛 결과가 나온다)
- **m01 의 환각 교훈** — 워커에 제목만 주면 없는 API 를 지어냈고, **근거를 함께 넘기고
  `evidence in note` 로 대조**하니 환각이 0 이 됐다. 아래 D4 가 이걸 따른다

리포에 **streamlit 선례는 없다.** 데모는 새로 설계한다.

---

## 설계 결정

슬라이스 안에서 흔들리지 않게 여기서 못박는다.

### D1. 근거 조회는 카테고리별 고정 도구 4종 — 벡터 검색을 쓰지 않는다

과제 지표가 *"실제로 호출한 도구 집합이 기대 도구와 정확히 일치하면 1점"* 이다.
도구 경계가 흐릿하면 이 지표를 잴 수 없다. **카테고리 = 도구** 로 1:1 대응시킨다.

| 도구 | 근거 |
|---|---|
| `lookup_food_type` | 식품공전 제5.9 음료류 (+ 제2. 식품일반 공통기준 일부) |
| `lookup_labeling` | LBL 표시기준 본문 + 별표 |
| `lookup_additive` | 식품첨가물공전 사용기준 + LBL 첨가물 표시 별표 |
| `lookup_ad_claims` | FLA/FLD 표시·광고법·시행령 + UNF 부당표시광고 고시 |

도구 내부 발췌는 **섹션 인덱스 + 키워드 매칭**으로 시작한다. m05 는 하이브리드(BM25+코사인)를
썼지만 여기서는 쓰지 않는다 — (1) 섹션 경계가 법령 구조로 이미 명확하고, (2) 첨가물은
품목명 **정확 매칭**이 맞지 근사 매칭이 틀리면 위험하며, (3) 재현 가능해야 채점기가 흔들리지 않는다.
**부족하면 BM25 를 개선 축 하나로 추가한다** (슬라이스 9의 후보). 처음부터 넣지 않는다.

### D2. 기대 근거는 **집합**으로 채점한다

PRD 7절에서 열어둔 것. "이 첨가물 써도 되고 라벨엔 뭐라 적나" 는 두 도구에 걸친다.
라우터가 복수 카테고리를 반환할 수 있게 하고, S1 은 `호출 집합 == 기대 집합` 으로 잰다.
과제 문구("도구 **집합**이 정확히 일치")와 그대로 맞물린다.
난도 관리를 위해 평가셋의 복수 카테고리 문항은 **2건까지**.

### D3. 넘기기는 **두 갈래**, 서로 다른 노드에서 판정한다

과제가 *"카테고리 선택과 넘기기 판단을 분리하라"* 고 못박았다.

- **`out_of_scope`** (UC-5) — 소관이 아님(HACCP·수입신고·처분 수위). **판정 노드**에서 거른다.
  근거를 조회하지 않는다 → 기대 도구 집합은 **공집합**
- **`no_evidence`** (UC-6) — 카테고리는 맞는데 발췌 범위에 답이 없음. **근거 조립 이후** 판정.
  → 기대 도구 집합은 **해당 도구 1개**

한 노드에서 처리하면 기대 도구를 정의할 수 없다. 이 구분이 S1 채점에 직접 들어간다.

### D4. 환각 검증은 규칙 기반 — LLM 판정을 쓰지 않는다

답변에서 **수치 · 조문 번호 · 식품유형명**을 뽑아 주입된 근거 텍스트에 실제로 있는지
문자열 대조한다(m01 의 `evidence in note` 와 같은 발상). LLM 판정기는 같은 입력에 다른 답을 내서
S3("근거 밖 진술 0건")을 잴 수 없게 만든다. 실패 시 **재생성 1회**, 그래도 실패하면 넘긴다.

### D5. 채점기는 지표마다 다르다 (m05 의 이중 층을 따른다)

- **S1** — 규칙 기반 집합 비교. LLM 불필요
- **S2** — LLM 채점기. 필수 사실을 **표현이 아니라 사실로** 보라고 프롬프트에 명시.
  JSON 스키마 강제 + 온도 0. 모범 답안으로 채점기 자체를 역검증한다 (S5)

### D6. 스택

`agentteam-m01` 관행 그대로 — **uv + 폴더 로컬 `.venv` + Python 3.13**, 활성화 없이 `uv run`.
`langgraph` · `langchain-openai` · `python-dotenv` 에 `streamlit`(데모)를 더한다.
법제처 수집은 표준 `httpx`/`requests` 로 충분하다(API 가 JSON 을 준다).
식품공전만 브라우저 렌더가 필요하다 — 슬라이스 2에서 방식을 정한다.

수집 원문·청크는 **커밋한다.** 사이트가 바뀌어도 구현과 채점이 계속 돌아야 한다.
파일마다 고시/법률 번호 · 시행일 · 수집일을 박는다.

### D7. m05 코퍼스를 복사하지 않고 다시 받는다

제출물이 **GitHub 주소 하나**이므로 이 폴더는 자립해야 한다. 그리고 m05 스냅샷은
2026-08-28 이라 이미 3주 지났다. 파싱 **규칙**은 가져오되 수집은 새로 한다.
범위는 우리 카테고리에 필요한 **4종(FLA · FLD · LBL · UNF)** 으로 줄인다.

### D8. 제2. 식품일반 공통기준은 **유형 판정에 쓰이는 것만** 도구에 넣는다

PRD 7절이 열어 둔 것. 슬라이스 2에서 제2 의 4항목(96청크·약 36,000자)을 다 받아 두고
여기서 연결할 자리를 정한다.

| 도구 | 제2 에서 가져가는 것 |
|---|---|
| `lookup_food_type` | **제2. 1. 식품원료 기준** (원료 구비요건·식품원료 판단기준) + **제2. 2. 제조·가공기준** |
| `lookup_additive` | **제2. 3. 의 「3) 식품첨가물」** 한 항목만 |
| (어디에도 안 넣음) | 제2. 3. 의 위생지표균·오염물질·기타 규격, 제2. 4. 보존 및 유통기준 |

제2. 2. 를 유형 판정에 붙이는 이유는 「'제5. 식품별 기준 및 규격'에서 원료배합시의 기준」이
거기 있기 때문이다 — **"유자즙 28%" 를 어떻게 세는지**가 UC-1 의 답을 좌우한다. 이게 없으면
답이 유형 정의 인용에서 멈춘다.

전부 넣지 않는 이유는 도구 1회 반환이 8,000자 이하여야 해서다. 36,000자를 넣으면 무엇을
자를지를 키워드 매칭이 정하게 되고, **재현 가능해야 채점기가 안 흔들린다**는 D1 의 전제가 깨진다.
넣지 않은 것들(위생지표균·오염물질·보존유통)은 PRD 3절의 어느 유즈케이스도 묻지 않는다.

제2. 1. 을 넣으면 UC-6 예시("유자 발효추출물을 식품원료로 쓸 수 있나요?")에서 도구가 비어 있지
않은 근거를 돌려준다. 이게 맞다 — D3 가 정의한 `no_evidence`(카테고리는 맞고 발췌 범위에 답이
없음, 기대 도구 1개)가 정확히 이 모양이다. 도구조차 안 잡히면 `out_of_scope` 로 새어 나간다.

### D9. 되묻지 않고 **조건부로 답한다**

PRD 7절이 열어 둔 것. UC-1 에서 과즙 함량 같은 판정 정보가 없을 때, 답을 미루고 되묻는 대신
**갈래를 다 보여주고 무엇이 더 필요한지 명시한다** —
"과·채즙 95% 이상이면 과·채주스, 10% 이상이면 과·채음료. 판정하려면 과즙 함량이 필요하다."

되묻기(1회)를 택하면 그 문항은 **되물었는가**만 채점하게 되어 S2(답변 적절성)가 그 문항에서
사실을 재지 못한다. 조건부 답변은 단발 질의 원칙을 유지하면서 "필요한 정보를 짚었는가"를
**필수 사실 하나로** 규칙 채점할 수 있다. 평가셋에 정보가 빠진 문항을 1건 넣는다.

### D10. `no_evidence` 는 답변 노드의 구조화 출력으로 판정한다 — 별도 게이트를 두지 않는다

D3 가 「근거 조립 이후 판정」이라고만 정하고 방법을 열어 뒀다. 슬라이스 7 착수 전에 닫는다.

- (A) 근거 충분성 전용 LLM 게이트 노드 — 호출이 1회 늘고, 게이트와 답변자의 판단이 갈릴 수 있다
- (B) **답변 노드를 구조화 출력(`Answer{verdict, text}`)으로 바꾼다** ← 채택
- (C) 규칙으로 판정 — 근거가 「답을 담고 있는지」는 키워드로 못 잰다. 불가

(B) 인 이유: 근거를 실제로 읽은 주체가 답변자다. 호출 수가 늘지 않고,
`verdict == "no_evidence"` 면 본문을 넘기기 문구로 **결정적으로** 교체하므로 S4 채점이 규칙이 된다.

`out_of_scope` 는 반대로 결정적이다 — `classify` 가 빈 집합을 돌려주면 `gather` 를 건너뛴다.
두 갈래가 서로 다른 노드에서 정해져야 기대 도구 집합(공집합 vs 1개)이 정의된다 (D3).

### D11. 환각 검증기의 추출 규칙과 면책 목록

D4 의 「수치·조문 번호·식품유형명을 근거와 문자열 대조」를 실행 가능한 규칙으로 못박는다.

| 뽑는 것 | 규칙 |
|---|---|
| 수치 | `\d[\d,.]*\s*(%\|퍼센트\|g\|㎎\|mg\|kg\|㎖\|mL\|L\|배\|일\|개월\|pt\|포인트)` |
| 조문 번호 | `제\d+조(의\d+)?(제\d+항)?(제\d+호)?` · `별표 \d+` · `별지 \d+` |
| 식품유형명 | 식품공전 음료류 유형 목록(고정 사전)에 있는 말 |

대조는 `tools.norm` 으로 부호·공백을 지운 뒤 **주입된 근거 텍스트 전체**(`ToolResult.text`)에 건다 —
슬라이스 4에서 금지 표현 대조에 쓴 것과 같은 정규화다. 청크 하나가 아니라 전체에 거는 이유는
답변이 두 청크를 합쳐 한 문장으로 쓰는 것이 정상이기 때문이다.

**면책 목록** — 잡으면 오탐이 된다:

- 질문에 이미 있던 토큰 (「유자즙 28%」를 답변이 되풀이하는 것)
- `[청크ID]` 인용 표기 자체
- 근거 블록 머리줄의 시행일 (`(시행 2026-01-01)`)

### D12. S2 채점은 두 층 — 필수 사실은 LLM, 금지 표현은 규칙

D5 의 이중 층을 지표에 맞춰 나눈다.

- **필수 사실** — LLM 채점기. JSON 스키마 강제 + 온도 0. 프롬프트에 「표현이 아니라 사실로 보라」 명시
- **금지 표현** — 규칙. `tools.norm` 정규화 후 부분 문자열 대조. LLM 을 태우지 않는다

한 문항 1점 = (필수 사실 전부 충족) **AND** (금지 표현 0건).

### D13. 개선 축은 넷으로 고정하고 기계가 센다

m05 의 `eval-run.mjs`(두 축 이상이면 `exit 2`)를 그대로 옮긴다. 축은 넷이다.

`router_prompt` · `answer_prompt` · `topic_index` · `model`

각 축의 버전 문자열을 `RunConfig` 로 모든 실행 기록에 박고, 직전 기록과 비교해 둘 이상 달라지면
실행을 거부한다. 과제의 「개선은 한 번에 하나씩」이 사람의 약속이 아니라 게이트가 된다.

---

## 1단계 완료 (2026-09-17, 슬라이스 1~5, 510f2c0~3053e64)

본문은 [PLAN-archive.md](PLAN-archive.md). 근거 4문서 수집(법제처 API·식품안전나라 공전) ·
조회 도구 4종 · 평가셋 18문항 · `판정 → 근거 조립 → 답변` LangGraph 관통까지 돌아간다.

### 1단계가 넘긴 것

슬라이스 5 관통 실측(`data/runs/slice5.json`, gpt-4o-mini, 18문항). 2단계가 이걸 출발점으로 쓴다.

| 신호 | 값 | 무엇을 뜻하나 |
|---|---|---|
| S1 (도구 집합 일치) | **15/18 = 0.83** | 이미 기준 0.75 를 넘었다. 틀린 셋은 전부 **라우터 과선택** (FT-1·AC-2·AC-3 이 도구를 하나 더 고름) |
| 지어낸 청크 ID | **0건** | 인용 형식은 이미 믿을 수 있다 |
| FT-4 (UC-6) | 근거 15청크를 받고 「사용할 수 있다」 = 이 문항의 금지 표현 | `no_evidence` 판정이 없어서 난 것 (슬라이스 7) |
| AC-4 (「무방부제」) | 근거 1청크 | 「무(無)」 주제 색인 공백 — 개선 축 후보 |

→ **S1 은 병목이 아니다.** 2단계의 실제 위험은 S2(답변 적절성) · S3(근거 밖 진술) · S5(채점기 역검증)다.
개선 3회를 S1 에 태우면 이미 넘은 수치를 다시 올리는 헛수고가 된다.

---

## 2단계 — 검증·측정·개선

끝나면 눈으로 확인할 것: **`uv run python evaluate.py` 한 줄이 S1~S4 수치와 문항별 오답 원인을
출력하고, 개선 3회의 축·수치 변화 기록표가 남는다.**

- [x] **6. 환각 검증 노드** `[어려움]` (2026-09-17 — `verify.py` · `docs/VERIFY-NOTES.md` ·
      `data/runs/slice6.json`) — `verify.py` 를 만들고 `agent.py` 그래프에 `answer → verify`
      를 붙인다 (D4·D11). 검증 실패 시 위반 목록을 프롬프트에 넣어 **재생성 1회**, 그래도 실패하면
      넘기기로 보낸다(슬라이스 7이 받는다).
      *완료 기준*: (a) `uv run python verify.py --selftest` 이 자작 답변 6종으로 규칙을 확인하고
      실패 시 종료 코드 1 — **잡아야 하는 것 4종**(함량 기준값 바꾸기 · 근거에 없는 조문 번호 ·
      없는 식품유형명 · 사용량 한도 바꾸기), **통과해야 하는 것 2종**(질문의 숫자를 되풀이한 답변 ·
      근거를 그대로 옮긴 답변), (b) `uv run python -m agent --goldenset` 기록에 문항별 위반 목록과
      재생성 여부가 남는다, (c) 18문항에서 **오탐 0건** — 오탐이 나면 규칙을 고치고 무엇이 왜 걸렸는지
      `docs/VERIFY-NOTES.md` 에 적는다, (d) 재생성이 걸린 문항은 전후 답변이 기록에 둘 다 남는다.

      **실측** — 자체 확인 6건 전부 의도대로(종료 코드 0), 관통 18문항에서 **대조 23건 · 위반 0 ·
      재생성 0 · 오탐 0**. 재생성은 관통에서 걸리지 않아 답변을 일부러 틀리게 만들어 확인했다
      (`answerHistory` 에 전후 둘 다 남는다). 8문항은 답변에 수치·조문·유형명이 없어 **대조할 것이
      0건**이었다 — 「위반 0」이 「답변이 근거 안에 있다」는 뜻은 아니다.
      규칙이 못 잡는 것 둘은 `docs/VERIFY-NOTES.md` 에 있다: 근거가 두 도구분이면 숫자가 **무관한
      조문에서** 발견돼 통과한다(실측: 「과·채즙 90%」가 카페인 조항의 `90%` 에 걸려 통과),
      **실재하지 않는** 식품유형명은 고정 사전에 없어 아예 뽑히지 않는다. 전자는 슬라이스 9의
      `라우팅 과선택` 분류가 S3 에도 영향을 준다는 뜻이다.

- [ ] **7. 넘기기 두 갈래 분리** `[선행: 6]` — `State.escalation` 을
      `None | "out_of_scope" | "no_evidence"` 로 두고, `out_of_scope` 는 `classify` 에서 결정적으로,
      `no_evidence` 는 답변 노드의 구조화 출력으로 정한다 (D3·D10). 슬라이스 6의 검증이 재생성
      뒤에도 실패하면 `no_evidence` 로 보낸다.
      *완료 기준*: (a) OS-1·OS-2 가 `out_of_scope` 이고 **호출 도구 0개**, (b) FT-4 가 `no_evidence`
      이고 **호출 도구 정확히 1개**(`lookup_food_type`), (c) 나머지 15문항의 `escalation` 이 전부
      `None` — 멀쩡한 문항이 넘기기로 새면 실패다, (d) 넘기기 문구에 **어디에 물어야 하는지**가
      들어간다 (UC-5·UC-6), (e) (a)(b)(c)를 `uv run python -m agent --goldenset` 이 직접 확인하고
      실패 시 종료 코드 1.

- [ ] **8. 채점기 + 모범 답안 + 역검증(S5)** `[선행: 7]` — `evaluate.py` 와
      `data/reference-answers.json`(18문항). S1 은 집합 비교, S2 는 두 층(D12), S3 는 슬라이스 6의
      검증기 재사용, S4 는 `escalation` 라벨 일치. 모범 답안은 평가셋의 `requiredFacts` 를 사실로
      담고 `gold` 청크만 인용한다 — 새로 쓰는 것이 아니라 **이미 원문에서 확인한 사실을 문장으로
      옮기는 일**이다.
      *완료 기준*: (a) `uv run python evaluate.py --reference` 가 모범 답안으로 **전 문항 S1~S4 1점**
      (= S5 통과), 실패 시 종료 코드 1, (b) 모범 답안이 `gold` 밖 청크를 인용하지 않고 `requiredFacts`
      를 전부 담는지 검증기가 확인한다, (c) 금지 표현 채점이 정규화 대조임을 **부호·공백을 일부러
      바꾼 표현**으로 확인한다(「과·채즙 50% 이상」 ↔ 「과채즙 50 % 이상」), (d) 채점 결과 JSON 에
      문항별 점수와 **왜 0점인지**가 들어간다.

- [ ] **9. 기준 측정 + 오답 원인 분류** `[선행: 8]` — 실제 파이프라인으로 18문항을 돌려 S1~S4 기준
      수치를 기록하고 실패 문항마다 원인을 분류한다. `RunConfig`(D13 의 네 축)를 여기서 정의해 모든
      기록에 박는다 — 슬라이스 10의 가드가 이걸 읽는다. 원인 분류는 여섯으로 고정한다:
      `라우팅 과선택` · `라우팅 누락` · `도구 범위 밖` · `발췌 누락` · `답변 누락` · `넘기기 오판`.
      *완료 기준*: (a) `uv run python evaluate.py --out data/runs/baseline.json` 이 S1~S4 를 출력하고
      기록한다, (b) 기록에 `RunConfig` 네 축의 버전이 들어간다, (c) 1점이 아닌 문항마다 원인 분류가
      붙는다, (d) `docs/BASELINE.md` 에 **개선 후보를 우선순위로** 적는다 — 슬라이스 10은 이 목록에서만
      고른다, (e) 이미 기준을 넘은 지표는 「넘었다」고 적고 개선 대상에서 뺀다.

- [ ] **10. 개선 3회 + 축 가드 (S6)** `[선행: 9]` — `evaluate.py` 가 직전 기록의 `RunConfig` 와 비교해
      **두 축 이상 바뀌었으면 종료 코드 2** (D13). 매회 재측정하고 `docs/IMPROVEMENTS.md` 에 기록표를
      남긴다. 후보는 슬라이스 9의 우선순위가 정하고, 지금 이미 보이는 것은 아래 셋이다.

      | 축 | 후보 | 근거 |
      |---|---|---|
      | `router_prompt` | 「필요 최소 집합만」 지시 + 과선택 예시 | FT-1·AC-2·AC-3 |
      | `topic_index` | 「무(無)」·「천연」 주제 색인 추가 | AC-4 근거가 1청크뿐 |
      | `answer_prompt` | 필수 사실 누락 자리 보강 | 슬라이스 9의 `답변 누락` 분류 |

      *완료 기준*: (a) 개선 3회의 실행 기록이 각각 파일로 있고 회차마다 **축이 정확히 하나만** 바뀐다,
      (b) 두 축을 바꾼 실행을 가드가 실제로 거부하는 것을 **한 번 눈으로 본다**(종료 코드 2),
      (c) 최종 수치가 **S1 ≥ 0.75 · S2 ≥ 0.75 · S3 위반 0건 · S4 위반 0건**, (d) 기록표에 회차별
      「바꾼 축 · 수치 변화 · 왜 그 축을 골랐나」, (e) 기준을 조정했으면 이유를 적는다
      (PRD 4절이 한 번 허용 — `REPORT.md` 로 옮긴다).

## 3단계 — 데모·리포트·제출

- **11. streamlit 데모** — 답변 + 호출 도구 + 인용 원문 + 검증 결과가 한 화면 (S7)
- **12. REPORT.md 7항목** — Mermaid 구조도 · 개선 기록표 · 화면 캡처
- **13. 제출 점검** — README 체크리스트를 실행으로 확인, 키 누출 점검

---

## 검증

각 슬라이스는 자체 완료 기준으로 검증한다. 단계 끝 검증은 이렇게 한다.

- **1단계**: `uv run python -m agent "<질문>"` 으로 답변 + 근거가 나온다 → 끝남
- **2단계**: `uv run python evaluate.py` 가 S1~S4 수치와 문항별 오답 원인을 출력하고,
  S1 ≥ 0.75 · S2 ≥ 0.75 · S3 위반 0건 · S4 위반 0건
- **3단계**: `uv run streamlit run app.py` 로 데모가 뜨고 임의 질문 1건이 근거와 함께 답변된다

LLM 호출이 필요한 슬라이스(5·6·8·9·10)는 **API 키가 있어야 검증된다.** 키 없이 돌린 경우
그 슬라이스는 미검증으로 남기고 체크하지 않는다.

**함정 셋** — 전부 조용히 틀린 결과를 낸다:

- LangGraph `compile()` 은 재정의된 함수를 다시 잡지 않는다. `StateGraph(...)` 부터 새로 만든다
- 코퍼스를 다시 수집하면 청크 ID 가 밀린다. 평가셋의 `goldCheck` 정규식이 이걸 잡는다
- **채점기를 모범 답안에 맞춰 고치면 S5 가 무의미해진다.** 역검증이 실패하면 고칠 곳은 채점기다
