# PLAN 아카이브 — 식품 표시·규격 라우팅 에이전트 (agentteam-m05)

> 끝난 단계의 본문을 여기로 옮긴다. `PLAN.md` 에는 한 줄 요약만 남는다.
> **자동화와 스킬은 이 파일을 읽지 않는다** — 사람이 옛 결정의 경위를 볼 때만 연다.

---

## 1단계 — 근거 문서와 평가셋

끝나면 눈으로 확인할 것: **질문 하나가 근거 조문을 달고 답변된다.**

- [x] **1. 프로젝트 셋업 + 법제처 API 수집** `[어려움]` — uv 프로젝트를 만들고,
      국가법령정보센터 OPEN API 로 FLA·FLD·LBL·UNF 4종을 받아 조/항/호 사실 단위로 청킹한다.
      `m05-rag-chatbot-service/app/scripts/fetch-corpus.mjs` 의 파싱 규칙을 Python 으로 옮긴다
      (`eflaw` 로 현행본만, 미시행은 알림만, 위치를 모르면 `본문`).
      *완료 기준*: (a) `uv run python collect_law.py` 가 에러 없이 끝나고 4종 청크 JSON 이 생긴다,
      (b) 청크마다 `lawName·path·url·effectiveDate·inForce` 가 비어 있지 않다,
      (c) 수집 리포트에 문서별 시행일과 청크 수가 찍힌다, (d) `inForce=false` 인 청크가 **0건**이다,
      (e) 표시기준 별표가 별도 파일로 저장된다.
      → 완료 (2026-09-17). FLA 64 · FLD 20 · LBL 114 · UNF 17 = 215 청크, 별표 16종.
      **m05 규칙을 옮기며 조용히 틀리던 두 자리를 고쳤다** — JS `\b`(ASCII) ≠ Python `\b`(유니코드)라
      고시 호 경계를 통째로 놓치던 것, 표시기준 53,216자가 문자열 하나로 와서 Ⅱ·Ⅲ 장이
      「Ⅰ. 총 칙」으로 찍히던 것. 기록은 `docs/COLLECT-NOTES.md`.

- [x] **2. 식품공전·첨가물공전 음료류 발췌** `[어려움]` — 법제처로는 못 받는 두 문서를
      식품안전나라(`various.foodsafetykorea.go.kr/fsd`)에서 받는다. 대상은 식품공전
      「제5. 9. 음료류」(+ 제2. 식품일반 공통기준 중 필요한 부분)와 첨가물공전의 해당 품목 사용기준.
      `/fsd/api/tree` 는 외부 호출 시 CORS 로 거부되므로 **페이지 렌더 후 본문을 긁는 방식**으로 간다.
      *완료 기준*: (a) 음료류 유형 정의가 **빈 문자열이 아니고** 과·채주스/과·채음료/혼합음료/액상차가
      전부 들어 있다, (b) 파일 첫머리에 고시번호·시행일·수집일·출처 URL 이 있다,
      (c) 첨가물은 품목명으로 사용기준 섹션이 잡힌다, (d) 취득 실패한 부분과 대체안을
      `docs/COLLECT-NOTES.md` 에 적는다.
      → 완료 (2026-09-17). `collect_code.py`. FDC 143 · FAC 883 = 1,026 청크, 첨가물 품목 716종.
      완료 기준 (a)(c)는 스크립트가 끝에서 직접 확인하고 실패하면 종료 코드 1.
      **여기서도 계획의 전제가 틀렸다** — CORS 는 브라우저 규칙이라 서버 호출을 막지 않고,
      렌더해도 긁을 HTML 이 없다(화면에 뜨는 것이 PDF 뷰어다). 공전은 항목마다 PDF 한 개라
      트리 API 로 fileId 를 받아 PDF 를 직접 받고 pdfplumber 로 **표를 칸 단위로** 뽑았다.
      줄 단위로 뽑으면 옆 품목의 사용량이 내 품목 밑에 붙는다. 기록은 `docs/COLLECT-NOTES.md`.

- [x] **3. 카테고리 확정 + 매핑표 + 조회 도구 4종** `[선행: 2]` — D1 의 도구 4종을 구현하고,
      카테고리를 문서의 어느 부분과 연결할지 매핑표를 만든다. 첨가물은 별칭 인덱스를 따로
      만든다("비타민C" / "아스코르브산" / "L-아스코르브산나트륨") — 청크의 `품목명 (영문명 이명 …)`
      머리줄이 별칭의 출처다.
      **제2 의 범위는 D8 이 정했다** — 결정을 다시 열지 말고 그대로 구현한다.
      *완료 기준*: (a) 도구 4종을 직접 호출해 **비어 있지 않은** 근거가 나온다,
      (b) "유자즙 28%" · "비타민C" · "면역력" 세 질의어로 각각 맞는 섹션이 잡힌다,
      (c) 매핑표가 `docs/CATEGORY-MAP.md` 에 있고 연결할 섹션이 없는 카테고리가 **없다**,
      (d) 도구 1회 반환이 **8,000자 이하** — 문서를 통째로 넣지 않는다.
      → 완료 (2026-09-17). `corpus.py`(별표를 본문과 같은 스키마의 청크로) + `tools.py`.
      범위 92 · 222 · 895 · 106 청크, 첨가물 별칭 색인 849행. 매핑표는 `docs/CATEGORY-MAP.md`.
      완료 기준 (a)(b)(c)(d)는 `uv run python tools.py` 가 질의 19건으로 직접 확인하고
      실패하면 종료 코드 1 — 매핑표에 빠진 도구·주제가 있어도 실패한다.
      **키워드 매칭만으로는 못 가는 자리가 있다** — 「면역력」은 수집한 법령 4종에 한 번도
      안 나온다. 그래서 주제 색인을 먼저 두고 키워드는 남은 자리를 채우게 했다. 기록은
      `docs/CATEGORY-MAP.md` 3·7절.

- [x] **4. 평가셋 12문항** — `data/goldenset.json`. 문항마다 `질문 · 기대 도구(집합) ·
      필수 사실 · 금지 표현 · gold 청크 ID · goldCheck 정규식`. **필수 사실은 전부 수집한 원문에서
      직접 확인하고 조문 위치를 같이 적는다** — 기억으로 쓰지 않는다. 금지 표현에는 문서를
      안 읽었을 때 나오기 쉬운 **그럴듯한 오답**(실제와 다른 함량 기준값·사용량 한도)을 넣는다.
      *완료 기준*: (a) 12문항 이상, 카테고리별 개수 차이 1건 이내,
      (b) 넘기기 정답 3건 이상(`out_of_scope`·`no_evidence` 각각 1건 이상), (c) 복수 카테고리 문항 2건,
      (b-2) **판정 정보가 빠진 문항 1건** — D9 에 따라 되묻기가 아니라 조건부 답변이 정답이고,
      "과즙 함량이 필요하다"를 짚는 것이 필수 사실 하나다,
      (d) 프롬프트 예시용 문항이 **별도 파일**에 있고 goldenset 과 겹치지 않는다,
      (e) 검증 스크립트가 스키마·분포·중복·**goldCheck 정규식 일치**를 확인하고 통과한다.
      → 완료 (2026-09-17). `data/goldenset.json` 18문항 + `data/prompt-examples.json` 3문항,
      검증기는 `validate_goldenset.py`. 도구별 5·5·4·4(차이 1), 넘기기 3건(out_of_scope 2 ·
      no_evidence 1), 복수 카테고리 2건, 조건부 답변 1건, 필수 사실 30개 · 금지 표현 55개 ·
      gold 청크 19개. 완료 기준 (a)~(e)는 `uv run python validate_goldenset.py` 가 직접 확인하고
      실패하면 종료 코드 1 — 검증기 자체도 goldCheck·금지 표현·분포를 일부러 깨서 잡히는 것을 봤다.
      **금지 표현을 눈대중으로 쓰면 원문에 실재하는 문장을 금지하게 된다** — 검증기가 둘을
      잡았다. 「살균제품임을 표시하여야 한다」는 같은 청크 안 발효음료류 규정이고, 「명칭과
      용도를 함께 표시하여야 한다」는 별지 1 의 별표 4 규정이다. 둘 다 주어를 붙여
      (「구연산은 …」) 틀린 주장으로 바꿨다. 그래서 금지 표현 대조는 `tools.norm` 으로
      부호·공백을 지우고 gold 본문에만 건다.
      **gold 도달률 23/23.** 첫 측정은 22/24 였고 못 물어 온 둘의 원인이 서로 달랐다.
      - `FLA-033`(AC-1) — **주제 색인 누락.** 「면역력」이 「질병효능」에만 있었고 그 주제는
        제8조제1항 제1호·제2호만 가리켰다. 면역력은 건강기능식품 고시의 기능성 표현이라
        제3호(건기식 오인)에 먼저 걸린다. 「건기식오인」 주제에 기능성 표현을 넣어 두 주제가
        같이 켜지게 고쳤다 (`tools.py`, 기록은 `docs/CATEGORY-MAP.md` 7절).
      - `LBL-F1-013`(AD-2) — **도구 범위 밖.** `lookup_additive` 의 LBL 범위가 `^별표 [456] `
        라 별지 1 은 0청크다. 범위를 넓히는 대신 gold 를 `LBL-T6-001` 로 좁혔다 — 별표 6 은
        **규칙이 표 제목에 있고**(「명칭, 간략명 또는 주용도를 표시하여야 하는 식품첨가물」)
        `_render` 가 `path` 를 근거와 함께 넘기므로 이 문항은 그것만으로 답이 선다. 별지 1 의
        단서가 필요한 자리는 MX-1 이 `lookup_labeling` 과 함께 시험한다. 검증기의 goldCheck 도
        도구가 실제로 넘기는 것(`path` + 본문)과 맞췄다.

- [x] **5. 최소 파이프라인 관통** — LangGraph 로 `판정 → 근거 조립 → 답변` 을 잇는다.
      검증·넘기기는 아직 없다.
      *완료 기준*: (a) `uv run python -m agent "유자즙 28% 음료를 유자주스로 팔아도 되나요?"` 가
      답변 + 호출 도구 + 인용 근거를 출력한다, (b) 평가셋 12문항을 전부 흘려 **에러 0**,
      (c) 답변에 인용 조문이 실제로 포함된다.
      → 완료 (2026-09-17). `agent.py` — 노드 셋(`classify` · `gather` · `answer`) + CLI.
      모델은 `gpt-4o-mini`(`OPENAI_MODEL` 로 교체), 온도 0, 판정은 구조화 출력(`Routing`).
      완료 기준 (a)는 위 명령 그대로 확인했고, (b)(c)는
      `uv run python -m agent --goldenset` 이 18문항을 흘려 **에러 0 · 인용 없음 0** 으로 직접
      확인하고 실패하면 종료 코드 1. 관통 기록은 `data/runs/slice5.json`.
      **인용 검사는 "대괄호가 있는가"가 아니라 "주입한 청크 ID 인가"로 건다** — 답변에서 뽑은
      ID 를 그 실행에 실제로 넣은 근거 ID 집합과 대조하고, 없는 ID 를 인용하면 에러로 센다.
      18문항 전부 지어낸 ID 0건이었다. 소관 밖 문항(OS-1·OS-2)은 근거가 0청크라 (c)를 걸지 않는다.
      **관통만 본 것이고 답이 맞는지는 보지 않았다** — 다음 슬라이스들이 잡을 자리가 이미 보인다:
      - `FT-4`(UC-6)가 근거에 없는 결론을 냈다 — 「사용할 수 있다」는 이 문항의 금지 표현이다.
        `no_evidence` 판정이 없으니 답변 프롬프트의 5번 지시만으로는 못 막는다 (슬라이스 6·7).
      - 라우터가 단일 카테고리 문항에서 도구를 하나 더 고른다 (`FT-1`·`AC-2`·`AC-3`).
        S1 이 집합 일치라 그대로면 0점이다 — 기준 측정(8) 뒤 개선 축 후보다.
      - `AC-4`(「무방부제」)는 근거가 1청크뿐이다. 「무(無)」 표시 주제 색인을 볼 자리.
